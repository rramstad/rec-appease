<resources xmlns:app="http://schemas.android.com/tools" xmlns:android="urn:oasis:names:tc:xliff:document:1.2">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/coral</item>
        <item name="colorPrimaryDark">@color/coral</item>
        <item name="colorAccent">@color/coral</item>

    </style>

    <style name="ActionBarTheme" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="background">@color/coral</item>


    </style>

    <style name="ActionBarOverlay">
        <item name="android:colorControlNormal">@color/coral</item>
    </style>

    <style name="ActionBarPopup">
        <item name="android:colorBackground">@color/coral</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="EditText" parent="Widget.AppCompat.EditText">
        <item name="android:fontFamily">@font/cafe</item>
        <item name="android:textSize">18dp</item>
    </style>

</resources>
